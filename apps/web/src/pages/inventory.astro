---
import Base from '../layouts/BaseLayout.astro';
import Inventory from '../react/Inventory';
import { RequireRole } from '../react/AuthContext';
---
<Base title="Inventario — Gemelli IT">
  <div class="flex items-center justify-between mb-4">
    <h1 class="text-2xl font-bold">Inventario</h1>
    <RequireRole role="LIDER_TI" client:only="react"><a class="bg-blue-600 text-white rounded px-3 py-2" href="/inventory/new">+ Nuevo</a></RequireRole>
  </div>
  <Inventory client:load />
</Base>
