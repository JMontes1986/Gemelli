---
import Base from '../../../layouts/BaseLayout.astro';
import LogForm from '../../../react/LogForm';
const { id } = Astro.params;
---
<Base title="Logs — Gemelli IT"><LogForm deviceId={id} client:load /></Base>
