---
import Base from '../../../layouts/BaseLayout.astro';
import SpecForm from '../../../react/SpecForm';
const { id } = Astro.params;
---
<Base title="Especificaciones — Gemelli IT"><SpecForm deviceId={id} client:load /></Base>
